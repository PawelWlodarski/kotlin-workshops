package lodz.jug.kotlin.reactive.typedworkshop

import io.kotlintest.specs.StringSpec
import io.kotlintest.matchers.shouldBe

class Part1TypedExercises : StringSpec() {

    //don't touch tests
    //complete actors at the bottom of the file
    init{
        "Calc should cache calculations"{

        }
    }
}